export interface SingleRecordProps {
    id: string,
    artist: string,
    name: string,
    year: number,
    genre: string,
    is_new: boolean,
    condition: string,
    quantity: number,
    is_available: boolean,
    image: string
}